```markdown
# Colorwalk 网页工具 — 项目开发指南

## 项目概述

Colorwalk 是一个移动端优先的纯前端网页工具，用于在温州七彩洞头村进行"色彩漫步"体验。用户通过转盘随机选取颜色，在村庄中寻找同色系的事物并拍照，生成色卡、制作照片拼贴，并在地图上标记发现色彩的地点。

目标用户：到访洞头村的游客，使用手机浏览器访问。
部署方式：纯静态文件，无后端，数据存储在用户本地浏览器中。

## 技术栈

- 框架：Vue 3 + Vite（单页应用）
- UI：自定义 CSS，不使用组件库，保持轻量和设计自由度
- 地图：Mapbox GL JS（底图设为浅灰色低饱和风格，突出彩色标记）
- 图片处理：Canvas API（色彩提取、拼贴合成、导出）
- 本地存储：IndexedDB（通过 idb-keyval 库简化操作，存储照片和色卡数据）
- 图标：Lucide Icons（线性风格）
- 字体：系统默认无衬线字体栈（-apple-system, "Noto Sans SC", sans-serif）

## 项目结构

```
src/
├── App.vue                 # 主布局 + 底部导航
├── main.js
├── router.js               # 路由配置
├── stores/                 # Pinia 状态管理
│   ├── colorStore.js       # 当前选中颜色、色卡集合
│   ├── photoStore.js       # 照片数据
│   └── mapStore.js         # 地图标记点
├── views/
│   ├── WatchColor.vue      # "观色"主页面
│   ├── Gallery.vue         # "画廊"页面
│   ├── MapView.vue         # "地图"页面
│   └── About.vue           # "七彩洞头村"介绍页
├── components/
│   ├── ColorWheel.vue      # 转盘组件
│   ├── CameraCapture.vue   # 拍照/上传组件
│   ├── ColorExtractor.vue  # 色彩分析组件
│   ├── ColorCard.vue       # 单张色卡展示
│   ├── CardNaming.vue      # 色卡命名交互
│   ├── CollageEditor.vue   # 拼贴编辑器
│   ├── CollageTemplates.vue# 拼贴模板选择
│   ├── MapMarker.vue       # 地图标记组件
│   └── NavBar.vue          # 底部导航栏
├── utils/
│   ├── colorUtils.js       # 颜色处理工具函数（提取主色、色差计算、HSL转换）
│   ├── storage.js          # IndexedDB 封装
│   ├── nameGenerator.js    # 色卡诗意名称生成器（预设词库）
│   └── exportImage.js      # Canvas 导出图片工具
├── assets/
│   ├── colors.json         # 12色转盘颜色定义（含洞头村特色色）
│   └── names.json          # 诗意颜色名称词库
└── styles/
    ├── variables.css       # CSS 变量（颜色、间距、圆角等）
    ├── global.css          # 全局样式重置
    └── animations.css      # 转盘旋转、页面切换动画
```

## 全局 UI 规范

### 色彩体系
- 背景：#FFFFFF（主）、#F5F5F5（次级）
- 文字：#1A1A1A（主）、#666666（次级）、#999999（辅助）
- 边框/分割线：#E5E5E5
- 强调色：不设固定强调色，由用户当前选中的颜色动态充当强调色
- 彩色元素仅出现在：转盘、色卡、地图标记、照片——其余界面保持黑白灰

### 布局
- 移动端优先，设计宽度 375px，最大宽度 430px 居中
- 底部固定导航栏，高度 56px，4个tab（观色、画廊、地图、关于）
- 页面内容区域 padding: 16px，距底部导航留 72px 安全距离
- 卡片圆角 12px，投影 0 2px 8px rgba(0,0,0,0.06)

### 字体
- 标题：18px / font-weight 600
- 正文：14px / font-weight 400
- 辅助文字：12px / font-weight 400 / color #999

### 图标
- 使用 Lucide Icons，线宽 1.5px，尺寸 24px（导航栏）/ 20px（页面内）
- 未选中状态 #999，选中状态 #1A1A1A

### 交互反馈
- 按钮点击：scale(0.96) + 100ms ease
- 页面切换：淡入淡出 200ms
- 转盘旋转：带物理惯性的减速动画

---

## 功能模块详细设计

### 一、观色（WatchColor）

这是核心功能页，包含完整的 Colorwalk 流程，以步骤引导的方式呈现。

#### 步骤 1：转盘定色

**交互设计：**
- 一个 12 色双层同心圆转盘，直径占屏幕宽度 80%
- 底层：固定的 12 色色环，每个扇区 30°，颜色来自 colors.json（基于洞头村实景提取的 12 个代表色）
- 顶层：可拖拽旋转的深灰色（#333）圆环遮罩，有一个 90° 的镂空窗口，露出下方 3 个连续色块
- 镂空窗口边缘有细微的白色描边，强调选中区域

**交互逻辑：**
- 支持触摸拖拽和鼠标拖拽旋转顶层遮罩
- 旋转时带物理惯性（松手后根据速度继续旋转并减速）
- 停止后自动吸附到最近的 30° 整数倍位置，确保窗口恰好覆盖 3 个完整色块
- 吸附时有轻微弹性动画

**结果展示：**
- 转盘下方实时显示当前窗口内的 3 个颜色，以 3 张小色卡的形式排列
- 每张色卡显示：色块 + 颜色名称 + HEX 值
- 底部有"开始漫步"按钮，点击后进入步骤 2，同时锁定当前颜色选择

**12色定义参考（可根据洞头村实际调整）：**
```json
[
  { "hex": "#C62828", "name": "渔船红" },
  { "hex": "#E65100", "name": "夕照橙" },
  { "hex": "#F9A825", "name": "渔灯黄" },
  { "hex": "#558B2F", "name": "海藻绿" },
  { "hex": "#00695C", "name": "礁石青" },
  { "hex": "#0277BD", "name": "东海蓝" },
  { "hex": "#283593", "name": "深港蓝" },
  { "hex": "#4527A0", "name": "贝壳紫" },
  { "hex": "#AD1457", "name": "晚霞粉" },
  { "hex": "#4E342E", "name": "石屋棕" },
  { "hex": "#37474F", "name": "瓦片灰" },
  { "hex": "#F5F5F5", "name": "浪花白" }
]
```

#### 步骤 2：漫步拍摄

**界面：**
- 顶部显示当前目标的 3 个颜色条（小色块横排）
- 中间区域是已拍摄照片的卡片流（竖向滚动）
- 底部固定一个圆形拍照按钮（相机图标）
- 拍照按钮旁有一个相册选择按钮（从已有照片选取）

**拍照/上传后：**
- 使用 Canvas 提取照片的前 5 个主色（K-means 或中位切分算法）
- 将提取的主色与目标 3 色做色差对比（使用 CIEDE2000 或简化的 HSL 距离）
- 在照片卡片上显示匹配度最高的颜色标签
- 照片卡片样式：照片 + 底部色条（显示提取出的主色）

**数据记录：**
- 每张照片记录：图片数据（压缩后的 base64 或 Blob）、提取的主色数组、拍摄时间、GPS 坐标（如用户授权定位）

#### 步骤 3：色卡生成

**触发：** 用户点击某张照片进入详情，或在拍摄完成后进入色卡编辑

**界面：**
- 照片居中大图展示
- 下方展示从该照片提取的 5 个主色，每个色块可点击
- 点击某个色块 → 生成一张色卡：
  - 色卡样式：上方大色块（占 60%）+ 下方信息区（颜色名称、HEX、拍摄地点）
  - 颜色名称默认由系统随机生成（从诗意名称词库中根据色相/明度匹配）
  - 用户可点击名称进行自定义编辑（inline 输入框）
- 色卡可保存到"画廊"

**诗意名称生成逻辑：**
- 预设词库按色相分类（红色系、蓝色系、绿色系等）
- 每个色相下有 20-30 个诗意名称
- 根据提取色的 HSL 值匹配最近色相分类，随机选取一个名称
- 示例：蓝色系 → "远山蓝"、"雨后天"、"旧信封的蓝"、"深巷尽头"

#### 步骤 4：照片拼贴

**入口：** 拍摄 3 张以上照片后出现"生成拼贴"按钮

**模板设计（提供 4-6 种）：**
- 模板 A：左侧大色块 + 右侧 2 张照片竖排
- 模板 B：顶部色条 + 下方 3 张照片横排
- 模板 C：九宫格（中间一格为色块，周围 8 格为照片）
- 模板 D：左右分栏，左 1 大图 + 右 2 小图 + 底部色条
- 模板 E：瀑布式，1 大 + 2 小 + 色卡信息条
- 模板 F：全出血大图背景 + 前景 3 个圆形裁切照片 + 色块点缀

**拼贴编辑器：**
- 选择模板后，照片自动填入（用户可拖拽调整顺序）
- 色块颜色取自当前目标 3 色中的主色
- 底部可添加文字水印："Colorwalk · 七彩洞头" + 日期
- "保存"按钮 → 使用 Canvas 合成为一张图片（1080x1350px，适合社交媒体）→ 触发下载

---

### 二、画廊（Gallery）

**布局：** 顶部 tab 切换 —— "色卡集" | "拼贴作品"

#### 色卡集 Tab
- 瀑布流展示所有已保存的色卡
- 每张色卡显示：色块 + 名称 + 采集日期
- 点击可查看大图和关联的原始照片
- 长按/右上角菜单可删除

#### 拼贴作品 Tab
- 瀑布流展示所有已保存的拼贴图
- 点击查看大图，可重新下载
- 显示创作日期和使用的颜色

**空状态：** 当没有数据时，显示引导插画 + "去观色"按钮

---

### 三、地图（MapView）

**地图配置：**
- 使用 Mapbox GL JS
- 底图样式：mapbox://styles/mapbox/light-v11（浅色底图）或自定义样式（去掉彩色 POI，只保留道路和建筑轮廓为浅灰色）
- 初始视角：七彩洞头村（北纬 27.819373°，东经 121.152969°），zoom 15
- 限制拖拽范围在洞头村附近（防止用户迷失）

**标记点：**
- 每个有 GPS 坐标的照片/色卡自动生成一个标记点
- 标记点样式：圆形，填充色为该照片/色卡的主色，直径 16px，白色描边 2px
- 点击标记点弹出气泡卡片：缩略图 + 颜色名称 + 时间
- 多个标记点按时间顺序用虚线连接，形成漫步路径

**手动打点：**
- 右下角浮动按钮"+ 标记"
- 点击后进入标记模式：地图中心出现十字准星
- 用户移动地图定位，点击确认位置
- 弹出选择器：从已有色卡中选一个关联到该位置，或直接选一个颜色
- 也可以在标记时拍照/上传照片

**路径回顾：**
- 顶部可按日期筛选，查看某一天的漫步路径
- 所有标记点形成的彩色路径就是用户的"色彩足迹"

---

### 四、七彩洞头村介绍（About）

**内容：**
- 顶部全宽 banner 图（洞头村全景）
- Colorwalk 玩法简介（图文结合，3-4 步图示）
- 洞头村简介（文字 + 图片轮播）
- 底部：项目信息、毕设署名

**首次访问：**
- 用户第一次打开网页时，默认进入此页面作为引导
- 看完后点击"开始 Colorwalk"跳转到观色页
- 之后再访问则默认进入观色页（用 localStorage 记录是否首次）

---

## 数据模型（IndexedDB）

```javascript
// 色卡
{
  id: string,          // uuid
  hex: string,         // 主色 HEX
  hsl: [h, s, l],     // HSL 值
  name: string,        // 用户命名或系统生成
  sourcePhotoId: string, // 关联照片 ID
  createdAt: timestamp,
  location: { lat, lng } | null
}

// 照片
{
  id: string,
  blob: Blob,          // 压缩后的图片数据
  thumbnail: string,   // 缩略图 base64（用于列表快速展示）
  dominantColors: [hex, hex, hex, hex, hex], // 提取的 5 个主色
  targetColors: [hex, hex, hex],             // 当次漫步的目标 3 色
  createdAt: timestamp,
  location: { lat, lng } | null
}

// 拼贴作品
{
  id: string,
  imageBlob: Blob,     // 合成后的图片
  templateId: string,  // 使用的模板
  photoIds: [string],  // 使用的照片 ID
  colors: [hex],       // 使用的颜色
  createdAt: timestamp
}

// 地图标记
{
  id: string,
  lat: number,
  lng: number,
  colorCardId: string | null,
  photoId: string | null,
  color: string,       // HEX
  createdAt: timestamp
}
```

## 性能注意事项

- 照片上传后立即压缩（最大宽度 1200px，JPEG quality 0.8）
- 缩略图单独生成（宽度 300px），用于列表展示
- 瀑布流使用虚拟滚动或懒加载，避免大量图片同时渲染
- 地图标记超过 50 个时使用聚合（cluster）
- Canvas 色彩提取在 Web Worker 中执行，避免阻塞主线程

## Mapbox Token

使用环境变量 VITE_MAPBOX_TOKEN 注入，在 .env 文件中配置。开发时使用免费 token。

## 开发顺序建议

1. 项目初始化 + 全局样式 + 导航栏 + 路由
2. 转盘组件（核心交互，优先打磨）
3. 拍照 + 色彩提取
4. 色卡生成 + 命名
5. 画廊页面
6. 拼贴编辑器
7. 地图页面
8. 关于页面
9. 首次访问引导
10. 整体联调 + 性能优化