```markdown
# Colorwalk 网页工具 — 完整开发指南（花园版）

## 项目概述

Colorwalk 是一个移动端优先的纯前端网页工具，让人们随时随地进行"色彩漫步"。灵感来自"陌上花开，可缓缓归矣"以及七彩洞头村被浙江省政府定位为"海上花园"的概念。

**核心隐喻：** 地图上的路径是"花园中的小路"，采集的色彩像花朵一样开在路边。用户的每一次漫步都在地图上留下一条彩色足迹，多次漫步后，整个地图变成一个绚烂的"色彩花园"。

**工作流程：** 用户打开应用即看到地图（显示历史路径和色彩点），点击"+ 新建路径"开始漫步，实时记录轨迹和采集色彩，结束后色彩点自动标记在地图上，点击色彩点可查看对应的照片和色卡。

目标用户：任何想要进行色彩漫步的人，使用手机浏览器访问。
部署方式：纯静态文件，无后端，用户数据存储在本地浏览器（IndexedDB），示范数据硬编码在项目中。

---

## 技术栈

- 框架：Vue 3 + Vite（单页应用）
- UI：自定义 CSS，不使用组件库，保持轻量和设计自由度
- 地图：Leaflet + OpenStreetMap（极简灰度滤镜底图，突出彩色路径和色彩点）
- 图片处理：Canvas API（色彩提取、拼贴合成、导出）
- 色彩计算：chroma-js（颜色插值、混合、色差计算、色相/明度/饱和度转换）
- 本地存储：IndexedDB（通过 idb-keyval 库简化操作）
- 图标：Lucide Icons（线性风格，线宽 1.5px）
- 字体：系统默认无衬线字体栈（-apple-system, "Noto Sans SC", sans-serif）

---

## 全局 UI 规范

### 色彩体系
- 背景：#FFFFFF（主）、#F5F5F5（次级）
- 文字：#1A1A1A（主）、#666666（次级）、#999999（辅助）
- 边框/分割线：#E5E5E5
- 强调色：不设固定强调色，由用户当前采集的颜色动态充当强调色
- 彩色元素仅出现在：色彩点、色卡、照片、拼贴——其余界面保持黑白灰

### 布局
- 移动端优先，设计宽度 375px，最大宽度 430px 居中
- 底部固定导航栏，高度 56px，3个 tab
- 页面内容区域 padding: 16px，距底部导航留 72px 安全距离
- 卡片圆角 12px，投影 0 2px 8px rgba(0,0,0,0.06)

### 字体
- 标题：18px / font-weight 600
- 正文：14px / font-weight 400
- 辅助文字：12px / font-weight 400 / color #999

### 图标
- 使用 Lucide Icons，线宽 1.5px，尺寸 24px（导航栏）/ 20px（页面内）
- 未选中状态 #999，选中状态 #1A1A1A

### 交互反馈
- 按钮点击：scale(0.96) + 100ms ease
- 页面切换：淡入淡出 200ms
- 弹出浮窗：缩放 + 淡入 200ms ease-out
- 色彩点点击：放大 + 高亮 150ms

---

## 项目结构

```
src/
├── App.vue
├── main.js
├── router.js
├── stores/
│   ├── walkStore.js        # 当前漫步状态（轨迹点、色彩节点、路径名称）
│   ├── galleryStore.js     # 画廊数据（所有漫步记录、色卡、拼贴）
│   └── mapStore.js         # 地图数据（所有历史路径和色彩点）
├── views/
│   ├── MapView.vue         # 开屏地图（主页面）
│   ├── AboutView.vue       # "关于"tab - Colorwalk 介绍 + 示范作品
│   ├── GalleryView.vue     # "画廊"tab - 用户的色卡和拼贴
│   └── WalkingView.vue     # 行进中页面（全屏，隐藏导航栏）
├── components/
│   ├── NavBar.vue          # 底部导航栏（关于 | + 新建 | 画廊）
│   ├── TrailMap.vue        # Leaflet 地图容器 + 路径和色彩点渲染
│   ├── ColorPointPopup.vue # 点击色彩点后的中间浮窗
│   ├── WalkingPanel.vue    # 行进中的操作面板
│   ├── CameraButton.vue    # 拍照按钮
│   ├── ColorPickerSheet.vue # 拍照后提色弹出面板
│   ├── PathNamingDialog.vue # 路径命名对话框
│   ├── WalkResult.vue      # 漫步成果页
│   ├── ColorCard.vue       # 单张色卡组件
│   ├── CollageEditor.vue   # 拼贴编辑器
│   ├── CollageTemplates.vue # 拼贴模板
│   └── DemoWalkDisplay.vue # 示范作品展示组件
├── utils/
│   ├── colorExtract.js     # 色彩提取算法（中位切分法）
│   ├── colorUtils.js       # 颜色工具（色差计算、HSL转换、匹配度）
│   ├── nameGenerator.js    # 色卡诗意名称生成器
│   ├── gps.js              # GPS 定位封装
│   ├── storage.js          # IndexedDB 封装
│   ├── imageCompress.js    # 图片压缩
│   └── exportImage.js      # Canvas 导出图片
├── data/
│   ├── demoWalk.js         # 展览示范数据（洞头村漫步完整记录）
│   └── demoPhotos/         # 示范照片（压缩后的 base64 或图片文件）
├── assets/
│   └── names.json          # 诗意颜色名称词库（按色相分类）
└── styles/
    ├── variables.css       # CSS 变量
    ├── global.css          # 全局样式重置
    └── animations.css      # 动画定义
```

---

## 导航结构

底部固定导航栏，3个 tab：

| 图标 | 标签 | 页面 | 说明 |
|------|------|------|------|
| info | 关于 | AboutView | Colorwalk 介绍 + 示范作品展示 |
| plus | + 新建 | 触发新建路径流程 | 开始一条新漫步 |
| image | 画廊 | GalleryView | 用户的色卡和拼贴作品 |

**首次访问逻辑：**
- 用户首次打开网页时，自动进入 MapView（开屏地图）
- 地图上显示示范作品的路径和色彩点
- 用 localStorage 记录首次访问状态

**行进中的特殊处理：**
- 用户点击"+ 新建"开始漫步后，隐藏底部导航栏
- 显示全屏的 WalkingView（地图 + 操作面板）
- 点击"结束漫步"后恢复导航栏，返回 MapView

---

## 核心流程详细设计

### 一、开屏地图（MapView）

这是应用的主页面，展示所有历史漫步的路径和色彩点。

**页面布局：**
```
┌─────────────────────────┐
│                         │
│                         │
│   OpenStreetMap         │
│   + 历史路径（彩色线条）  │
│   + 色彩点（圆点）       │
│                         │
│                         │
├─────────────────────────┤
│  关于    + 新建    画廊   │  ← 导航栏
└─────────────────────────┘
```

**地图配置：**
- Leaflet + OpenStreetMap，CSS 灰度滤镜底图
- 初始中心：用户当前位置（如果有定位权限）或默认中心
- 初始 zoom：15-16（街道级别）
- 限制拖拽范围（可选，防止用户迷失）

**路径渲染：**
- 每条历史漫步由多段 L.polyline 渐变色带组成
- 颜色：该次漫步的主导色或渐变色
- 宽度：6-8px，圆头
- 透明度：0.7（不要太浓，避免遮挡地图）
- 点击路径可高亮，显示该次漫步的基本信息（日期、时长、色彩数）

**色彩点渲染：**
- 每个采集的颜色是一个圆点标记
- 大小：固定直径 12px
- 颜色：采集的实际颜色
- 描边：白色 1px（提高对比度）
- 点击触发 ColorPointPopup 浮窗

**地图交互：**
- 可缩放和拖拽
- 点击路径 → 高亮该路径，显示路径信息
- 点击色彩点 → 弹出 ColorPointPopup
- 长按色彩点 → 显示色卡详情（可选）

---

### 二、关于 Tab（AboutView）

**页面结构：**
```
┌─────────────────────────┐
│                         │
│   Colorwalk 是什么？      │
│   （文字介绍 + 插画）     │
│                         │
├─────────────────────────┤
│                         │
│   怎样进行 Colorwalk？    │
│   （4步图示）            │
│                         │
├─────────────────────────┤
│                         │
│  ┌─────────────────┐   │
│  │ [查看示范作品]   │   │  ← 进入 DemoWalkDisplay
│  │ 洞头村·2024.05  │   │
│  └─────────────────┘   │
│                         │
├─────────────────────────┤
│  关于    + 新建    画廊   │
└─────────────────────────┘
```

**内容分段：**
1. **Colorwalk 介绍**（200-300字）
   - 什么是 Colorwalk
   - "花园中的小路，色彩像花朵一样开在路边"的概念
   - 这个工具的意义

2. **玩法说明**（图文结合）
   - 步骤1：点击"+ 新建"开始漫步
   - 步骤2：步行寻找色彩
   - 步骤3：拍照采色
   - 步骤4：色彩点自动标记在地图上

3. **示范作品入口**
   - 卡片样式：背景图（洞头村风景）+ 标题 + 日期 + 简要信息
   - 点击进入 DemoWalkDisplay

---

### 三、示范作品展示页（DemoWalkDisplay）

完整呈现设计者在洞头村的一次 Colorwalk 成果。

**页面布局（纵向滚动）：**
```
┌─────────────────────────┐
│                         │
│  洞头村 Colorwalk        │
│  2024.05.19             │
│  32分钟 · 1.6km · 6色   │
│                         │
├─────────────────────────┤
│                         │
│   [地图 + 色带路径]      │  ← 完整的漫步路线可视化
│   （可交互）             │
│                         │
├─────────────────────────┤
│  采集的色卡              │
│  [卡1] [卡2] [卡3] ...  │  ← 横向滚动
│                         │
├─────────────────────────┤
│  拍摄的照片              │
│  [照1] [照2] [照3] ...  │  ← 网格展示
│                         │
├─────────────────────────┤
│  生成的拼贴作品          │
│  [拼贴1] [拼贴2]        │
│                         │
├─────────────────────────┤
│  这是一个示范作品        │
│  展示 Colorwalk 的完整流程 │
│                         │
│  [返回地图]              │
│                         │
└─────────────────────────┘
```

**交互设计：**
- 地图区域：显示完整的色带路径（使用 MapComponent 组件）
- 色卡展示：横向滚动，点击展开详情
- 照片展示：网格或瀑布流，点击全屏查看
- 拼贴作品：展示生成的拼贴图，点击全屏查看
- 底部按钮："返回地图" → 回到 MapView

---

### 四、新建路径流程

**触发：** 用户点击导航栏的"+ 新建"按钮

**步骤1：路径命名（PathNamingDialog）**
```
┌─────────────────────────┐
│  给这条路径起个名字      │
│                         │
│  ┌─────────────────┐   │
│  │ 输入框（可选）   │   │
│  │ 默认：今天的漫步  │   │
│  └─────────────────┘   │
│                         │
│  [取消]    [开始]       │
└─────────────────────────┘
```

- 弹出对话框，要求用户给这条路径命名
- 提供默认名称（如"2024.05.19 的漫步"）
- 用户可自定义或使用默认
- 点击"开始" → 进入行进中页面

**步骤2：行进中（WalkingView）**

**页面布局：**
```
┌─────────────────────────┐
│  路径名称                │  ← 顶部显示当前路径名
├─────────────────────────┤
│                         │
│                         │
│     实时地图             │  ← Leaflet 地图 + 实时轨迹
│     + 当前路径（灰色）    │
│     + 已采集色彩点       │
│                         │
│                         │
├─────────────────────────┤
│  已采集 3 色 · 00:12:34  │  ← 状态信息栏
├─────────────────────────┤
│  [结束]    (📷)    [相册] │  ← 底部操作栏
└─────────────────────────┘
```

注意：此状态下隐藏底部导航栏。

**中部地图区域：**
- Leaflet 地图占满中间区域
- 底图样式：极简灰度
- 初始中心：用户当前 GPS 位置
- 初始 zoom：17（街道级别）
- 实时跟踪用户位置，地图跟随移动

**路径渲染逻辑：**
- 用户开始行走，GPS 轨迹实时记录
- 路径显示为浅灰色虚线（宽度 4px，#CCCCCC）
- 每确认一个色彩节点，该节点处显示一个小圆点（直径 10px，采集的颜色）
- 已采集的色彩点在地图上实时显示

**状态信息栏：**
- 显示：已采集 X 色 · 时长 HH:MM:SS
- 时长实时更新（每秒）
- 高度 36px，字号 13px，居中

**底部操作栏：**
- 高度 72px，白底，顶部 1px 分割线
- 三个按钮水平排列：
  - 左：结束按钮（小号，线框样式，灰色文字"结束"，防误触设计）
  - 中：拍照按钮（大圆，直径 56px，黑色实心，白色相机图标，居中突出）
  - 右：相册按钮（小号，线框样式，图标"从相册选"）
- 点击"结束"弹出确认对话框："结束本次漫步？"确认/取消

**GPS 轨迹记录：**
- 使用浏览器 Geolocation API 的 watchPosition
- 配置：enableHighAccuracy: true, maximumAge: 3000, timeout: 10000
- 节流：每 3 秒记录一个轨迹点
- 路径平滑：使用移动平均滤波（取最近 3 个点的平均值）
- 静止检测：连续 5 个点位移 < 3 米，判定为静止，暂停记录
- 数据自动保存：每 30 秒将当前轨迹存入 IndexedDB

---

### 五、拍照后提色弹出面板（ColorPickerSheet）

**触发：** 用户点击拍照按钮 → 调用系统相机 → 拍照完成后自动弹出此面板
或用户点击相册按钮 → 选择照片后自动弹出此面板

**面板样式：**
- 从底部滑入，覆盖屏幕约 65% 高度
- 背景：白色，顶部圆角 16px
- 上方有一个灰色拖拽条（4px × 40px，居中）
- 面板弹出时，后方行进页面加暗色遮罩（rgba(0,0,0,0.4)）

**面板布局：**
```
┌─────────────────────────┐
│          ──              │  ← 拖拽条
├─────────────────────────┤
│  ┌───────────────────┐  │
│  │                   │  │
│  │    照片预览        │  │  ← 照片，圆角8px，宽度90%，高度自适应（最大200px）
│  │                   │  │
│  └───────────────────┘  │
├─────────────────────────┤
│                         │
│  提取色彩：              │
│  (●)  (●)  (●)  (●)  (●) │  ← 5个提取色圆点，直径 36px
│                         │
│  点击选择要保留的颜色     │  ← 提示文字，12px，#999
│                         │
├─────────────────────────┤
│  已选中：                │
│  [██ XXX蓝] [██ XXX绿]  │  ← 实时显示已选中的色卡预览
│                         │
├─────────────────────────┤
│        [ 确认 ]          │  ← 按钮，黑底白字
└─────────────────────────┘
```

**交互逻辑：**
- 照片上传后，Canvas 提取 5 个主色（使用中位切分法）
- 5 个色块初始状态为"未选中"（圆点有细灰色边框）
- 用户点击某个色块 → 变为"选中"状态（放大到 42px + 白色描边 + 下方出现 ✓）+ 微弹动画
- 再次点击 → 取消选中
- 下方"已选中"区域实时更新，显示将要生成的色卡
- 点击"确认"：
  1. 选中的颜色生成色卡，存入 IndexedDB
  2. 照片存入 IndexedDB
  3. 当前 GPS 位置记录为色彩节点
  4. 面板收起，回到行进中页面
  5. 地图上新增一个色彩点（实时显示）

**色彩提取算法：**
- 将照片绘制到 Canvas（缩放到 100x100）
- 使用中位切分法（Median Cut）将颜色空间递归分割为 5 个区域
- 取每个区域的平均色作为主色
- 按照在图片中的占比从大到小排列

---

### 六、色彩点弹出浮窗（ColorPointPopup）

**触发：** 用户在地图上点击一个色彩点

**浮窗样式：**
- 中间浮窗，宽度 80%，最大宽度 320px
- 背景：白色，圆角 12px
- 投影：0 4px 16px rgba(0,0,0,0.15)
- 弹出动画：缩放 + 淡入 200ms ease-out
- 点击浮窗外的区域可关闭

**浮窗布局：**
```
┌─────────────────────────┐
│  ┌───────────────────┐  │
│  │                   │  │
│  │    照片预览        │  │  ← 照片，圆角8px，宽度100%
│  │                   │  │
│  └───────────────────┘  │
├─────────────────────────┤
│                         │
│  [██ 渔船红]            │  ← 色卡预览（色块 + 名称）
│  HEX: #C62828           │
│  RGB: 198, 40, 40       │
│                         │
├─────────────────────────┤
│  采集时间：2024.05.19 14:32 │
│  位置描述：渔港的红色渔船   │
│                         │
├─────────────────────────┤
│  [查看详情]  [关闭]      │  ← 按钮
└─────────────────────────┘
```

**内容：**
- 照片预览（缩略图）
- 色卡信息（色块 + 名称 + HEX + RGB）
- 采集时间
- 位置描述
- 两个按钮："查看详情"（进入色卡详情页）、"关闭"

**交互：**
- 点击"查看详情" → 进入色卡详情页（显示完整信息 + 可编辑名称）
- 点击"关闭"或浮窗外 → 关闭浮窗，回到地图

---

### 七、漫步成果页（WalkResult）

**触发：** 用户点击"结束漫步"并确认后，自动跳转到此页面

**页面布局（纵向滚动）：**
```
┌─────────────────────────┐
│                         │
│   路径名称               │
│   2024.05.19             │
│   32分钟 · 1.6km · 6色   │
│                         │
├─────────────────────────┤
│                         │
│   [地图 + 路径]          │  ← 完整的漫步路线可视化
│                         │
├─────────────────────────┤
│  本次色卡                │
│  [卡1] [卡2] [卡3] ...  │  ← 横向滚动色卡列表
│     点击名称可编辑        │
├─────────────────────────┤
│                         │
│  [返回地图]  [制作拼贴]  │  ← 两个按钮并排
│                         │
├─────────────────────────┤
│  关于    + 新建    画廊   │  ← 导航栏恢复显示
└─────────────────────────┘
```

**地图区域：**
- 显示完整的漫步路径（灰色线条）+ 所有色彩点
- 自动缩放到路径的 bounding box，留 padding
- 起点标记：小圆点 + "起"
- 终点标记：小圆点 + "终"

**色卡列表：**
- 横向滚动，每张色卡宽度 120px
- 色卡样式（Pantone 风格）：
  - 上方：大色块区域（80px 高）
  - 下方：白底信息区
    - 颜色名称（14px，可点击编辑）
    - HEX 值（12px，#999）
- 点击名称 → 变为输入框，可自定义编辑
- 名称旁有一个小骰子图标 🎲 → 点击随机换一个诗意名称

**按钮：**
- "返回地图"：回到 MapView，新的色彩点已显示在地图上
- "制作拼贴"：跳转到拼贴编辑器，自动载入本次照片

---

### 八、画廊 Tab（GalleryView）

**页面结构：**
```
┌─────────────────────────┐
│                         │
│  我的色卡                │
│  [卡1] [卡2] [卡3] ...  │  ← 简单列表或网格
│                         │
├─────────────────────────┤
│                         │
│  我的拼贴                │
│  [拼贴1] [拼贴2] ...    │  ← 简单列表或网格
│                         │
├─────────────────────────┤
│  [+ 创作新拼贴]          │  ← 按钮
│                         │
├─────────────────────────┤
│  关于    + 新建    画廊   │
└─────────────────────────┘
```

**色卡展示：**
- 简单列表或网格展示所有已收集的色卡
- 每张色卡显示：色块 + 名称 + 采集日期
- 点击可查看大图和关联的原始照片
- 长按/右上角菜单可删除

**拼贴展示：**
- 简单列表或网格展示所有已保存的拼贴作品
- 点击查看大图，可重新下载
- 显示创作日期

**创作新拼贴：**
- 底部"+ 创作新拼贴"按钮
- 点击进入拼贴编辑器

**空状态：**
- 当没有色卡时，显示引导文字"开始一次漫步，采集你的第一张色卡"
- 当没有拼贴时，显示引导文字"用照片创作拼贴吧"

---

### 九、拼贴编辑器（CollageEditor）

**入口：**
- 漫步成果页 → "制作拼贴"
- 画廊 tab → "+ 创作新拼贴"

**流程：选模板 → 选照片 → 预览 → 保存**

**步骤1：选模板**
- 横向滚动展示 4-6 种模板缩略图
- 模板设计：
  - A：左侧大色块（占1/3宽）+ 右侧 2 张照片竖排
  - B：顶部色条 + 下方 3 张照片横排
  - C：九宫格（中间一格为色块，周围为照片）
  - D：1 大图 + 2 小图 L 形排列 + 底部色条信息栏
  - E：上下两张大图 + 中间窄色条分隔
  - F：3 张照片斜切拼接 + 角落色块点缀
- 点击选中，进入下一